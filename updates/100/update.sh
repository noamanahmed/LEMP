#!/bin/bash

DIR=$(dirname "${BASH_SOURCE[0]}") 
DIR=$(realpath "${DIR}") 

template_path="$(cd $DIR/../../ && pwd)/templates"
source $DIR/../../includes/helpers.sh


if [ -f "$LEMP_FLAG_DI/ADMINER_INSTALLED"]
then

fi


if [ -f "$LEMP_FLAG_DI/COMPOSER_INSTALLED"]
then

fi


if [ -f "$LEMP_FLAG_DI/CRON_INSTALLED"]
then

fi


if [ -f "$LEMP_FLAG_DI/DOCKER_INSTALLED"]
then

fi


if [ -f "$LEMP_FLAG_DI/FAIL2BAN_INSTALLED"]
then

fi


if [ -f "$LEMP_FLAG_DI/JAVA_INSTALLED"]
then

fi


if [ -f "$LEMP_FLAG_DI/MONGODB_INSTALLED"]
then

fi


if [ -f "$LEMP_FLAG_DI/MONIT_INSTALLED"]
then

fi


if [ -f "$LEMP_FLAG_DI/PHPMYADMIN_INSTALLED"]
then

fi


if [ -f "$LEMP_FLAG_DI/PHPPGADMIN_INSTALLED"]
then

fi


if [ -f "$LEMP_FLAG_DI/PHP_INSTALLED"]
then

fi


if [ -f "$LEMP_FLAG_DI/PYTHON_INSTALLED"]
then

fi


if [ -f "$LEMP_FLAG_DI/SUPERVISOR_INSTALLED"]
then

fi


if [ -f "$LEMP_FLAG_DI/UFW_INSTALLED"]
then

fi


if [ -f "$LEMP_FLAG_DI/VIRTUALENV_INSTALLED"]
then

fi


if [ -f "$LEMP_FLAG_DI/ZABBIX_INSTALLED"]
then

fi


