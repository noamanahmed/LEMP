#!/bin/bash

DIR=$(dirname "${BASH_SOURCE[0]}") 
DIR=$(realpath "${DIR}") 

template_path="$(cd $DIR/../ && pwd)/templates"
source $DIR/../includes/helpers.sh

if [ "$EUID" -ne 0 ]
  then echo "Please run as root"
  exit
fi


while [ $# -gt 0 ]; do
  case "$1" in
    -u|--username)
      username="$2"
      shift
      ;;
    -p|--php)
      php="$2"
      shift
      ;;
    -a|--app)
      app="$2"
      shift
      ;;
    *)
      printf "***************************\n"
      printf "* Error: Invalid argument. $1 *\n"
      printf "***************************\n"
      exit 1
  esac  
  shift
done


if [ -z "$username" ]
then
    echo "Please provide a username using -u "
    exit
fi


if ! id "$username" &>/dev/null
then
    echo "The $username doesn't exist"    
    exit;
fi

chroot_path=/var/chroot/
jk_jailuser -m -j $chroot_path --shell=/bin/bash $username


cp -rf /root/.nvm /var/chroot/home/$username/.nvm

mkdir -p /var/chroot/home/$username/.ssh
mkdir -p /var/chroot/home/$username/.wp-cli

cp -rf $template_path/jailed_ssh/.ssh/* /var/chroot/home/$username/.ssh
cp -rf $template_path/jailed_ssh/.bashrc /var/chroot/home/$username/.bashrc
cp -rf $template_path/jailed_ssh/.profile /var/chroot/home/$username/.profile

chown -R $username:$username /var/chroot/home/$username/.wp-cli
chown -R $username:$username /var/chroot/home/$username/.ssh
chown -R $username:$username /var/chroot/home/$username/.bashrc
chown -R $username:$username /var/chroot/home/$username/.profile
#chown -R $username:$username /var/chroot/home/$username/.config
chown -R $username:$username /var/chroot/home/$username/.nvm

mkdir /var/chroot/home/$username/tmp
chown -R $username:$username /var/chroot/home/$username/tmp

sed -i "s/$(echo $chroot_path | sed 's/\//\\\//g').\/home\/$username/\/home\/$username\//" /etc/passwd
sed -i "s/\/usr\/sbin\/jk_chrootsh/\/bin\/bash/" /etc/passwd

